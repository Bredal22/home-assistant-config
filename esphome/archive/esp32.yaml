esphome:
  name: "esp32"

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:


wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esphome-Web-05871C"
    password: "r0b5377AmmCA"

captive_portal:

binary_sensor:
  - platform: gpio
    pin: GPIO23
    name: "PIR Sensor"
    device_class: motion